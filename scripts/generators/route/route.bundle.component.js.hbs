export const {{componentName}} = () => {
  return (
    <Bundle load={ load{{componentName}} }>
      { {{componentName}} => { 
        return (
          <div>
            {
              {{componentName}}
              ? <{{componentName}} />
              : <p>Loading</p>
            }
          </div>
        )
      } }
    </Bundle>
  )
}

/* BUNDLE GENERATOR */